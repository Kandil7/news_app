import 'package:flutter/material.dart';

class Settings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      "Setting Page",
      style: TextStyle(
          color: Colors.yellow, fontWeight: FontWeight.bold, fontSize: 40),
    ));
  }
}
